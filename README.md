# Markdown Previewer:

### Description: An application where users can type Markdown text on one side and see the rendered HTML preview on the other.
### Focused on Handling text input, creating a simple custom parser, and dynamically updating the preview area.
### This project involved a good understanding of manipulating text content and updating the DOM. 